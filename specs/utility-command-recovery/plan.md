# Plan

1. Reproduce both run-98 failures from the durable job and tool journals.
2. Reject unprofiled command packets with an exact resubmission contract.
3. Normalize focused-check argv path operands through the same verified
   registered-worktree root used for scopes and cwd.
4. Narrow oversized dynamic reads with explicit continuation metadata while
   preserving exact-read fail-closed behavior.
5. Add route, workspace, broker, and Pi replay regressions.
6. Run focused and full verification, then obtain exact-SHA review before any
   deploy.
