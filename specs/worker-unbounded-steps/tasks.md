# Tasks

- [ ] RED: `maxSteps` absence assertion fails on current code.
- [ ] GREEN: budget removed; runtime-bounded loop.
- [ ] Suites green at baseline; build + diff-check clean.
- [ ] Deploy (merge + rebuild, no restarts) and record eval.
