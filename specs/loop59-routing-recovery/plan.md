# Plan

1. Add one shared bridge parser for structured execution metadata and exact
   context-reference validation.
2. Feed parsed metadata into the existing request factory; keep deterministic
   router and broker validation authoritative.
3. Update bridge and paired-agent guidance with valid/invalid packet examples
   and safe decomposition rules.
4. Inspect real Au Pair rejection traces, then make the smallest feedback or
   repetition-control correction supported by that evidence.
5. Add focused contract, routing, Pi-harness, and live-process reload tests.
6. Run focused tests, full per-file suite, build, repository verify wrapper,
   and record an eval before live installation.
7. Merge locally into the runtime branch, install, reload only bridge and
   supervisory/helper processes, and send one read-only Loop-59 canary.

