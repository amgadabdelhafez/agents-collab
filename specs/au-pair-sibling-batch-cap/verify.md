# Verify

- Run `bun run test:file -- tests/loop/utility-pi-harness.test.ts`.
- Run `bun run test:ci`.
- Run `bun run check`, `bun run build`, and `git diff --check`.
- Confirm the deployed-lineage guard symbols remain present.
