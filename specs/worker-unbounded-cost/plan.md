# Plan

1. Remove worker-specific dollar caps from runtime configuration and routing.
2. Remove per-call and cumulative worker token limits.
3. Update runtime, router, help text, and regression coverage.
4. Run focused and full verification, build, merge locally, and respawn only
   the live governess pane.
