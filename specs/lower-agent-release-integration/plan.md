# Plan: Lower-Agent Release Integration

1. Record loop-40 pane identity and create an isolated Harness run.
2. Commit the already-verified component worktrees locally.
3. Merge both component branches and resolve shared bridge surfaces.
4. Run combined focused, full, build, and formatting verification.
5. Exercise default and opt-out pane geometry in disposable tmux sessions.
6. Merge locally, back up the installed binary, and replace it atomically.
7. Reconfirm loop-40 process and pane identity without interacting with it.
