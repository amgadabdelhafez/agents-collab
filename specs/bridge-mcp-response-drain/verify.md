# Verification

- A mutation restoring parent-loss exit after EOF fails a named test.
- A short-lived initialize plus send_message process returns both response IDs.
- Exactly one durable message is stored for the tool call.
- Focused bridge tests, complete sequential suite, check, build, TypeScript,
  Harness verification, preflight, and stop-gate pass.
