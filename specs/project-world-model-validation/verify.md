# Verification

- Each required guard has a distinct test whose assertion names the exact error.
- Persisted corrupt rows fail when read, not only when inserted through APIs.
- Focused World Model and CLI suites pass.
- Complete sequential suite, check, build, targeted TypeScript, Harness
  verification, preflight, and stop-gate pass.
