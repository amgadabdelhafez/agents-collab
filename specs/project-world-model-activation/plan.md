# Plan

1. Define the default-on activation, bounds, artifact binding, and authority
   boundary.
2. Add a run-scoped materializer that derives deterministic task seeds, writes
   an atomic bootstrap capsule, and records its manifest binding.
3. Invoke preparation before promptless and task-bound paired tmux handoff.
4. Add World Model verification and query guidance to both agent charters and
   export artifact paths to governed pane environments.
5. Verify producer-backed materialization, launch ordering, fail-closed
   behavior, manifest validation, charter guidance, and pane environment.
6. Pass focused tests, static checks, full tests, build, Harness preflight, and
   stop-gate; commit and request exact-SHA supervisor review.
