# Plan

1. Start an isolated worktree at exact concurred source `c842fac`.
2. Apply the reviewed context-pressure commit and resolve overlaps by retaining
   both the pressure trigger and every newer handover guard.
3. Add producer-backed coverage for the combined pressure and restart-epoch
   path plus an asymmetric `driverEffort=high` handover fixture.
4. Run focused tests, repository checks, build, full sequential tests, and the
   governed task verifier.
5. Commit the fresh lineage, request exact-SHA supervisor review, and stop.
