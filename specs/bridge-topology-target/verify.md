# Verification

- The bridge MCP returns invalid params for Gemini in a Claude/Codex manifest.
- No `bridge.jsonl` message is written for the rejected target.
- Existing bridge tests continue to pass, including topology-free fixtures.
