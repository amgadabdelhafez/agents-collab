# Plan

1. Derive write-target state while building the immutable utility context capsule.
2. Tell edit helpers how to handle capsule targets marked `new`.
3. Let `search_repo` treat an absent exact declared write target as an empty search domain while preserving all other path errors.
4. Add broker and context regressions, then run focused and estate verification.
