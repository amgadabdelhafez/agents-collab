# Plan: Aggressive Au Pair Routing

1. Add an explicit persisted utility-audit review mode while retaining the
   existing peer-verdict default.
2. Extend boundedness and verified-workspace handling for safe read-only
   utility audits, then deterministically classify them to Au Pair.
3. Narrow Nanny to at most two-scope/two-stage inspection synthesis so broader
   reasoning-backed work reaches Au Pair.
4. Align startup/channel/tool-schema guidance around utility audits, small edit
   packets, final main-agent judgment, and provider-neutral requests.
5. Add positive and negative router, tier, bridge-schema, runtime, and prompt
   regressions before running the repository's canonical verifier.
