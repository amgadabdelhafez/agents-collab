# Verification

- Focused utility workspace, delegation, hook, observability, and governess
  tests pass.
- Full `bun test` is compared with the known four-failure baseline.
- `bun run build`, changed-file Biome, and `git diff --check` pass.
- Live loop 47 journals one new linked-worktree worker job and renders the new
  considered/skipped reasons.
- Claude and Codex pane PIDs do not change.
- A peer verdict is sent through the bridge once; prompts prohibit parallel
  direct tmux injection and no longer request periodic receive polling.
