# Phase 0B Plan

1. Preserve the exact reviewed Phase 0A commit as the implementation base.
2. Add one pure `management-state.ts` module beside the Phase 0A contracts.
3. Implement lifecycle, policy, shift, deadline, and handoff semantics in one
   source/test edit batch.
4. Run one focused test file, scoped strict typecheck, scoped static check,
   build smoke, and diff check.
5. Obtain one native zero-write review and make only corrections required by a
   concrete finding.
6. Write `runs/agent-operations-phase0b-management/eval.json`, commit the
   bounded slice, and stop for independent root review.

No loop launch, service install, live adapter, backlog repair, merge, global
install, or remote push is part of this plan.
