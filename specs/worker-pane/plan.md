# Plan

1. Extend the read-only observability snapshot with delegation totals and
   skipped-reason aggregation.
2. Consolidate worker usage and routing rows into the governess board.
3. Redesign the worker renderer as a colored output-only transcript.
4. Apply a stable `worker.<session>` tmux pane title.
5. Update focused tests, run the full verification path, obtain independent
   evaluation, integrate locally, and refresh only loop 47 control panes.

