# Plan

1. Replace output-settling readiness with a positive Claude main-prompt
   predicate and a bounded fail-closed wait.
2. Supply `220x60` only as the paired-session fallback when no valid terminal
   dimensions exist.
3. Add unit regressions for delayed confirmation, timeout/no-paste, ordering,
   fallback geometry, and valid-terminal preservation.
4. Extend the realistic launch smoke so detached geometry and autonomous
   bootstrap are positively asserted.
5. Run focused and full verification, build exact bytes, obtain independent
   review, and pass the liaison section-4 gate before deployment.
