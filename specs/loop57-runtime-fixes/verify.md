# Verification

## Automated

- Focused tests cover each required behavior and fail closed on malformed input.
- Full `bun test` and `bun run build` pass.
- Harness verification, preflight, stop gate, and `eval.json` pass.

## Live Loop 57

- Claude/Codex pane ids and pids remain unchanged.
- No repeated `route-selected` collision appears across multiple Governess ticks.
- Eligible Au Pair work can start while Nanny is full.
- Helper failure output includes an exact broker code/message, and a sibling
  failure batch gets an adaptation turn.
- Pending helper results are drained by a same-requester delegation.
- Governess shows the Loop 57 execution objective.
- Recon 1/2/3 appear as the bottom row and show route/tool/result data.
