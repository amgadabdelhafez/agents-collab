# Plan

1. Add a small pure predicate for unprofiled bounded inspections beside the
   existing execution-tier classifier.
2. Add replay-style classifier tests and runtime persistence/no-fallback proof.
3. Run focused and full verification, build the compiled binary, and obtain an
   independent evaluator verdict.
4. Install the verified binary and replace only Loop 56's Governess/helper
   control processes while preserving Claude and Codex.
5. Route one harmless bounded inspection and verify Nanny usage evidence.
