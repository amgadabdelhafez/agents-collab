# Recon Pane Labels

## Required behavior

- The first read-only recon pane is titled `routes.<session>`.
- The second read-only recon pane is titled `tools.<session>`.
- The third read-only recon pane is titled `results.<session>`.
- Pane order, contents, refresh behavior, and stored pane IDs remain unchanged.
- Live Loop 57 labels are updated without restarting Claude or Codex.
