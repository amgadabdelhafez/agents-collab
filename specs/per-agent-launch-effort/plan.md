# Plan

1. Parse and validate global and per-role effort inputs before startup actions.
2. Resolve driver and reviewer effort with documented CLI and environment precedence.
3. Carry the resolved values through paired preparation into provider command construction and the manifest.
4. Add regressions for medium defaults, global override, role override precedence, asymmetric launch commands, invalid values, and manifest evidence.
5. Run focused and certified verification, record the eval, commit, and request exact-SHA supervisor review.
