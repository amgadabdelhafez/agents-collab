# Plan

1. Reuse the bridge runtime's existing SGR-dim parser instead of enumerating
   suggestion strings.
2. Add an optional styled-capture dependency and provide it in live defaults.
3. Normalize only the handover composer-safety probe through the dim parser.
4. Add positive ghost-text and negative real-draft regressions.
5. Select the latest hook other than `SubagentStop` or Claude's exact generic
   idle notification as the parent-turn boundary while keeping every other
   trailing event fail-closed.
6. Add a producer-backed regression at the post-bundle governed-exit consumer,
   retaining the permission-notification negative regression.
7. Verify, commit, and request exact-SHA supervisor review. Do not deploy.
