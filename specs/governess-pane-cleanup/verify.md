# Verification

- Focused Governess tests assert the cleaned tables, bridge labels, routing
  reason summaries, helper metrics, and Nanny model/runtime lines.
- Every rendered line fits the configured viewport width.
- The loop build and Harness gates pass.
- Live Loop 57 shows the cleaned board with no repeated frames in history.
- All non-Governess pane PIDs remain unchanged after deployment.
