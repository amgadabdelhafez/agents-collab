# Plan

1. Add regression tests for semicolon/mixed read plans, literal labels, and
   fail-closed stage handling.
2. Extract a strict leading-worktree prefix parser and test registered,
   unrelated, substituted, and unsafe-remainder cases through the hook path.
3. Extend utility observability with stable derived performance, context, and
   failure metadata.
4. Render the new worker detail rows in the governess board with bounded width.
5. Run focused and full tests, build, static checks, terminal-render capture,
   and an independent review.
6. Install the verified binary atomically for future loops without restarting
   the live Claude or Codex panes.

