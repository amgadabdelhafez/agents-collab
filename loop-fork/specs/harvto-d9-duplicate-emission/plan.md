# D9 Plan

1. Enforce Required behavior 10 across this complete five-file contract. Freeze all hashes only
   after planning edits stop; re-derive them immediately before one fresh Claude zero-write
   full-file review at exact base `18a590608b335f6a7637bf8215c4fe0508301282` and on verdict
   receipt. Only literal `PLAN PASS` naming that base and every current hash grants source/test/red
   authority.
2. At unchanged production bytes, add only
   `D9 resolved acknowledgement emits once across supersession retry and replay` to
   `tests/loop/governess-p0-runtime.test.ts`. Recreate the Run197 four-predecessor sequence through
   real enqueue, append-only journal/transcript reconstruction, consume, and post-delivery retry.
   Base must append and deliver four identical-signature acks rather than one. Freeze the exact
   command, test diff, fixture, output, event IDs/order, journal/transcript counts, hashes, and Git
   state in the six-file red directory.
3. If and only if exact-base red reproduces, add a narrow ack-identity helper in
   `bridge-store.ts`. It compares only explicit `type: "ack"` message rows by normalized signature,
   subject, reply/task/thread correlation, and ordered artifacts. It must exclude random ID,
   timestamp, priority, expiry/retention, and per-predecessor dedupe key.
4. Reorder only the necessary enqueue decision seam: discover the earliest matching valid ack in
   full reconstructed journal order as the semantic canonical ack; preserve same-key pending
   duplicate behavior; supersede a different exact pending predecessor once; then return the
   canonical ack as `duplicate` without a new message/transcript append. First acceptance continues
   through existing queue and append logic.
5. Prove each crash boundary by replaying from durable rows: pre-append crash permits one later ack;
   post-append retry, reconstructed restart, post-consume retry, and delivered replay all retain one
   message and one transcript emission. Four distinct predecessor keys all become superseded.
6. Add controls proving same-key ack replay does not supersede itself; changed body, subject,
   reply/task/thread, artifact list, source, or target remains distinct; repeated non-acks remain
   independent; explicit legacy ack rows fence replay; untyped legacy rows do not.
7. Run the named regression and focused control files, then mandatory check, canonical TypeScript,
   build, and plain `bun run test:ci`. Generate both evals only from fresh green evidence; validate
   both empty allowlists, run Harness preflight/stop-gate, and use the root verifier as the sealing
   repeat.
8. Reconcile normal and ignore-all-space numstats over exactly the one production and one test path.
   Commit only that implementation scope, obtain Claude exact-SHA literal `PASS`, close Harness
   exactly once, and commit lifecycle/evidence/ledger bookkeeping separately. Obtain exact
   bookkeeping-SHA `BOOKKEEPING PASS` before governed handover.

If unchanged base does not reproduce four effective ack emissions, preserve `not-reproduced` and
stop. Do not weaken distinct-message behavior, add a migration or side ledger, mutate Run197,
widen into D10-D12, or infer review authority from silence.
