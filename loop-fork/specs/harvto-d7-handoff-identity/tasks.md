# D7 Tasks

- [x] Validate the D6 terminal/bookkeeping boundary and empty index.
- [x] Promote parked D7 exactly once and reconcile its immutable intake with the canonical defect
  source.
- [x] Trace run-manifest, effective-model, handoff-digest, replacement-argv, and acceptance owners.
- [ ] Enforce Required behavior 11: after planning edits stop, freeze all five hashes together and
  obtain one fresh Claude literal zero-write `PLAN PASS` from a full review of all five complete
  files at the exact base; any planning-byte change or invalid verdict requires complete re-freeze.
- [ ] Preserve the named exact-base end-to-end parse/`Options`/effective-model red proving hostile
  ambient `luna` replaces source `sol`, without production edits.
- [ ] Persist and restore normalized effective launch identity without silent default resolution.
- [ ] Declare the shared launch-identity type in `src/loop/run-state.ts` within frozen scope.
- [ ] Digest-bind source identity and pass role-correct models into the fresh replacement launch.
- [ ] Reject parent-provable Claude-primary inexpressibility before spawn; reject replacement
  binary/default drift before acceptance or old-loop teardown.
- [ ] Add positive, mismatch, legacy, tamper, workspace, restart, replay, and isolation controls.
- [ ] Pass focused and mandatory suites with both evals and empty baseline failures/allowlist.
- [ ] Commit exact implementation scope and obtain Claude literal exact-SHA `PASS`.
- [ ] Close Harness once and commit bookkeeping separately; leave D8 parked.
