# D8 Tasks

- [x] Validate Run87 identity, handover boundary, utility `0/off/0`, frozen D6/D7 evidence, exact
  HEAD, empty index, and inherited dirty scope.
- [x] Promote D8 exactly once and prove all 63 prior task records and 12 non-D8 parked rows are
  unchanged.
- [x] Trace current epoch, route epoch, claim epoch, broker mutation, and application journal owners.
- [ ] Freeze all five contract hashes and obtain Claude literal five-file `PLAN PASS` for exact
  base; this is separate from the root-plan verdict.
- [ ] Preserve the exact-base two-file changed-epoch red with no production edit.
- [ ] Add an owned async authority lock that spans the awaited broker mutation and stays non-stale.
- [ ] Reuse internal `recordUtilityPatchApplicationLocked`; never re-enter the non-reentrant store
  lock.
- [ ] Add joint negative assertions: pre-broker rejection, zero application event, both bytes
  unchanged.
- [ ] Prove contended activation cannot interleave or return false: it advances after release or
  throws the distinct busy error under the existing deadline.
- [ ] Preserve one valid two-file apply and same-authority `already-applied` replay.
- [ ] Keep D10 preimage/applicability, linked-worktree, routing, claiming, and bridge controls green.
- [ ] Pass focused and mandatory suites, dual baseline checks, Harness gates, and root verifier.
- [ ] Commit only the exact two-production/two-test scope and obtain Claude exact-SHA `PASS`.
- [ ] Close D8 exactly once, commit bookkeeping separately, and hand over before D9.
