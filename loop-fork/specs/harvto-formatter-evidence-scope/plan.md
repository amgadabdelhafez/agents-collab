# Formatter Evidence Scope Plan

1. Revalidate run-78 handoff digests and fields, world-model bootstrap bindings, exact HEAD/parent,
   empty index, Git-derived dirty scope, D6 sole-active/eval-pending state, utility `0/off/0`, root
   `.loop/` count/tracking, frozen D6 contract hashes, ignored-plan provenance, and complete D6/D15
   evidence SHA-256/size inventories. Persist the observations under this task run.
2. Freeze this contract under Claude literal zero-write `PLAN PASS`. Record exact hashes for the
   four canonical contract files and run plan. No red or config edit starts before that verdict.
3. Attempt `./harness promote harvto-formatter-evidence-scope --mode maintenance` exactly once while
   D6 remains active. Capture pre/post Harness hashes and state. Any partial mutation stops without
   repair. A clean rejection completes standalone structure and stops for supervisor ratification.
4. If admission or later explicit ratification permits continuation, force-add only the five exact
   reviewed contract files, file by file. Prove ignore/no-rule provenance, exact cached path/hash
   equality, no `.loop/`, source, test, config, or evidence path, then create a dedicated plan-freeze
   commit and restore an empty index.
5. Record the formatter implementation base. Bracket read-only verbose Biome reproduction with full
   D6/D15 evidence inventories. Preserve exact command, output, exit status, checked-file list/count,
   resolved config evidence, config hash, HEAD, and index. The red must name pristine generated
   `loop-fork/runs/**` JSON inspected by current config.
6. Add deterministic controls: generated-run exclusion and byte identity; source/test temporary
   invalid-fixture rejection; bidirectional checked-file inventory with zero additions and removals
   equal only the supported `loop-fork/runs/` intersection; four-override/rule byte identity; exact
   local single-negative array; representative supported fixtures for every inherited exclusion
   class; and proof root `runs/**` is outside the Biome root. Record installed parent entries
   without copying or pinning them. Remove the invalid fixtures and preset-scope matrix entirely
   within this step, then prove every control path absent and status residue-free before any
   mandatory gate.
7. Edit only `loop-fork/biome.jsonc`, adding exactly top-level
   `"files": { "includes": ["!runs"] }` with the single-line array required by formatter output.
   Add no local positive glob and copy no preset entry. Run only read-only Biome commands, including
   a clean check of `biome.jsonc` itself. Compare frozen contract and evidence inventories
   immediately after the edit and every check.
8. After recording that both invalid fixtures and the complete preset-scope matrix are absent,
   status/cache residue-free, run focused controls, targeted Biome, `bun run check`, canonical
   TypeScript, `bun run build`, complete certified serial `test:ci`, dual passing evals with empty
   named allowlist and `baseline_failures`, Harness preflight/stop-gate when represented, and root
   `scripts/verify.sh`.
9. Prove exact Git scope and stage only `loop-fork/biome.jsonc`. Require one cached path, matching
   normal/ignore-all-space numstats, cached diff check, clean fixture residue, preserved evidence,
   utility `0/off/0`, and unchanged D6 lifecycle. Create one production commit without amend/rebase.
10. Obtain Claude literal zero-write exact-SHA `PASS`. On `REVISE`, change only authorized formatter
    scope, rerun all required gates, make a new explicit commit, and seek a new verdict.
11. Never run `harness done` for this unadmitted formatter task. After exact-SHA `PASS`, prove D6 is
    sole active/eval-pending, record exactly one standalone terminal `PASS` in formatter task-log,
    meta, and both evals, re-prove D6 state, and commit bookkeeping separately. Return to unchanged
    D6 only after the formatter gate is fully satisfied.
