# D10 Plan

1. Enforce Required behavior 11 across the complete five-file contract. Freeze all hashes only
   after planning edits stop; re-derive them immediately before one fresh Claude zero-write
   complete-file review at exact base `1282bd4c39ec7b3c0177810b30dab78ca6d66f41` and on verdict
   receipt. Only literal `PLAN PASS` naming that base and all five hashes grants source, test, red,
   eval, or staging authority.
2. At unchanged production bytes, add only
   `D10 guarded apply rejects absent null-preimage target before mutation and journaling` to
   `tests/loop/utility-runtime.test.ts`. Build a completed edit through real proposal creation and
   utility store authority with one absent exact target, a valid creation patch, matching artifact
   hashes, and a null preimage. Base must create the file and record one `patch-applied` event.
   Freeze the exact command, test diff, fixture, patch, output, observations, hashes, and Git state
   in the six-file red directory, then obtain Claude literal zero-write `RED VALID` before any
   production edit.
3. If and only if exact-base red reproduces, change only `src/loop/utility-tools.ts`. At the new
   application seam after integrity/scope/manifest validation and replay handling, require a
   non-null expected preimage and an exact current digest before `git apply --check`. Report the
   normalized target and expected/current digest-or-absent states without file contents for
   preimage rejection and for applicability rejection after an exact preimage comparison.
4. Preserve the existing `git apply --check`, post-check preimage revalidation, real apply,
   postimage capture, and runtime record ordering. Do not add a creation mode, manifest field,
   journal event, migration, or runtime/store source branch.
5. Add broker controls in `tests/loop/utility-tools.test.ts` for absent/null preimage, removed target,
   changed bytes with exact digest evidence, clean preimage but non-applicable patch, zero real apply
   on every rejection, and successful existing-target apply. Extend the real runtime regression and
   existing controls only as needed to prove target absence, zero application events, one valid
   apply, one replay, postimage drift, and unchanged D8 actor-authority ordering.
6. Run the named regression and the four reviewed focused files. Re-run the frozen red test after
   the fix and require pass while all six red files remain byte-identical and validate 5/5. Then run
   mandatory check, canonical TypeScript, build, and plain serial `bun run test:ci`.
7. Generate task-local and repository-root evals only from fresh green evidence. Require
   `baseline_failures: []`, no by-name allowlist entries, both baseline checks, Harness preflight and
   stop-gate from `loop-fork/`, and the repository-root verifier as a sealing repeat.
8. Reconcile normal and ignore-all-space numstats over exactly
   `loop-fork/src/loop/utility-tools.ts`, `loop-fork/tests/loop/utility-tools.test.ts`, and
   `loop-fork/tests/loop/utility-runtime.test.ts`. Commit only those paths and obtain Claude literal
   exact-SHA `PASS`. Close D10 exactly once, commit contract/lifecycle/evidence/ledger bookkeeping
   separately, obtain exact-SHA `BOOKKEEPING PASS`, and complete governed handover.

If unchanged base does not reproduce the declared creation and journal event, preserve honest
`not-reproduced` evidence and stop. Do not weaken authority or guards, edit frozen red, widen source
scope, absorb D8/D9/D11/D12, or infer any verdict from silence.
