# D10 Tasks

- [x] Validate Run93 manifest/topology, Run92 handover, utility `0/off/0`, exact HEAD, empty index,
  sealed D9, frozen evidence, and inherited dirty scope.
- [x] Promote D10 exactly once and prove every prior task, non-D10 parked row, coordination prefix,
  debt row, scratch file, and preserved local scope remains exact.
- [x] Trace runtime authority and journaling, broker proposal/apply ordering, target extraction,
  preimage/postimage checks, Git applicability, and existing broker/runtime controls.
- [ ] Freeze all five contract hashes and obtain Claude literal five-file `PLAN PASS` for exact base
  before any source, test, red, eval, or staging write.
- [ ] Preserve genuine exact-base absent-target creation and one application event as six-file red.
- [ ] Obtain Claude literal `RED VALID` for the frozen test diff and six-file evidence.
- [ ] Reject null-preimage and missing exact targets before either Git apply command.
- [ ] Emit exact normalized target plus expected/current digest-or-absent evidence without bytes.
- [ ] Preserve changed-target, non-applicable, concurrent-drift, and no-journal fail-closed ordering.
- [ ] Preserve valid existing-target apply, same-application replay, and postimage verification.
- [ ] Keep hash, manifest, exact-scope, dependency, symlink, D8 authority, workspace, and bridge
  utility controls green.
- [ ] Re-run frozen red after the fix and prove six files remain byte-identical and validate 5/5.
- [ ] Pass all focused and mandatory suites, dual empty-baseline checks, Harness gates, and root
  verifier with terminal evidence.
- [ ] Commit only the exact one-production/two-test implementation scope and obtain Claude exact-SHA
  literal `PASS`.
- [ ] Close D10 exactly once, commit bookkeeping separately, obtain `BOOKKEEPING PASS`, and hand over
  without starting D11/D12.
