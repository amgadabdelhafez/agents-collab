# Run Plan — harvto-d10-guarded-apply

Base: `1282bd4c39ec7b3c0177810b30dab78ca6d66f41`

1. Freeze this complete five-file contract and obtain fresh Claude literal `PLAN PASS` naming exact
   base and all five hashes before source, test, red, eval, or staging work.
2. Preserve genuine exact-base red through the real utility proposal, authority, guarded apply, and
   journal path: one absent exact target with null preimage is currently created and journaled.
3. Obtain `RED VALID`, then make only `utility-tools.ts` require an existing, exact preimage before
   either Git apply command. Report exact path and expected/current digest-or-absent state only.
4. Add broker diagnostics/order controls and the real runtime no-mutation/no-journal regression;
   preserve valid apply/replay, D8 authority, and all existing hash/scope/applicability protections.
5. Run focused and mandatory gates, re-run frozen red without changing its six files, generate and
   validate both empty-baseline evals, pass Harness gates, and use the root verifier as sealing
   repeat.
6. Commit only the exact one-production/two-test implementation scope and obtain Claude exact-SHA
   `PASS`. Close D10 once, commit bookkeeping separately, obtain exact-SHA `BOOKKEEPING PASS`, and
   complete governed handover without starting D11/D12.

D10 does not mutate Harvto or prior frozen evidence, reopen D8/D9, enable utility, alter runtime or
store source, change dependencies/providers/models, perform remote/release work, or widen into
D11/D12.
