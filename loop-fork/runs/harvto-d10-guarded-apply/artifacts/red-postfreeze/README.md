# D10 authoritative post-freeze red evidence

Task: `harvto-d10-guarded-apply`

Regression: `D10 guarded apply rejects absent null-preimage target before mutation and journaling`

This is the authoritative recovery capture produced only after plan-freeze commit
`e632e54ffa1239ada679abf55eca75471e2ce851`, whose parent is exact pre-freeze base
`1282bd4c39ec7b3c0177810b30dab78ca6d66f41`. The plan-freeze commit contains only the five PLAN-PASS contracts.
Production at the commit and in the live worktree remains byte-identical to the pre-freeze base.

The approved test-only diff creates one live-authority completed utility edit for absent exact target
`src/new.ts`, using a valid creation patch, broker-produced manifest, correct artifact hashes, and
proposal preimage `null`. The command selected one test and filtered 57. It exited 1 because
unchanged production returned `applied`, created the target with SHA-256
`a568bdcc8ccc6cc56a201d8eee207a4ec38998aa78a0ad4edd66472ec50d7e41`, and appended exactly one `patch-applied` event. Full event order,
authority epochs, artifact hashes, application pre/postimages, target state, and genuine-red
predicates are recorded in `fixture.json`.

Capture start: `2026-08-18T13:37:32Z`
Capture end: `2026-08-18T13:37:33Z`
Command cwd: `/Users/amgad/dev_projects/agents-collab-harvto-supervisor-defects/loop-fork`
Terminal exit: `1`

`fixture.patch` is the exact test-only diff against the plan-freeze commit. The earlier
`artifacts/red/` directory is separate quarantined ordering-incident evidence and was not written,
renamed, or regenerated by this capture. A separate recovery manifest binds both directories after
independent checksum validation. This directory has exactly six files; `SHA256SUMS` covers the
other five and must validate 5/5.
