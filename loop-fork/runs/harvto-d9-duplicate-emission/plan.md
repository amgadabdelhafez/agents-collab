# Run Plan — harvto-d9-duplicate-emission

Base: `18a590608b335f6a7637bf8215c4fe0508301282`

1. Freeze this complete five-file contract and obtain fresh Claude literal `PLAN PASS` naming the
   exact base and all five hashes before source, test, or red work.
2. Preserve genuine exact-base red from four Run197-shaped predecessor keys resolved by one ack
   body through real enqueue, journal/transcript reconstruction, consume, and delivered replay.
3. Add an ack-only semantic identity in `bridge-store.ts` from normalized signature plus exact
   correlation metadata, excluding random ID and predecessor dedupe key.
4. Resolve each predecessor once while appending one canonical ack/transcript row. Same-key,
   post-append, reconstructed, post-consume, and delivered retries return that row as `duplicate`
   without self-supersession or another delivery.
5. Preserve distinct acknowledgements, repeated non-acks, explicit legacy acks, untyped legacy
   messages, D1 QoS, D4 peer results, D5 completion, MCP dispatch, and inbox ordering.
6. Run focused and mandatory gates in order, generate and validate both empty-baseline evals, pass
   Harness gates, and use the root verifier as a sealing repeat.
7. Commit only the exact one-production/one-test implementation scope, obtain Claude exact-SHA
   `PASS`, close once, then commit bookkeeping and obtain exact-SHA `BOOKKEEPING PASS` before
   governed handover.

D9 does not mutate imported Run197, reopen D8, begin D10-D12, enable utility, alter bridge schemas,
or perform Harvto/remote/release work.
