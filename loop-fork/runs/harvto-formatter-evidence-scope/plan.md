# Run Plan — harvto-formatter-evidence-scope

Initial evidence HEAD: `254e1749ca67ad1d81cd434ef01db0aeabae5827`

1. Preserve exact run-78 handoff, bootstrap, Git, D6 lifecycle, utility, root `.loop/`, frozen D6
   hashes, ignored-plan provenance, and complete D6/D15 evidence inventories.
2. Attempt concurrent Harness promotion once with exact pre/post mutation evidence. On clean
   refusal, complete standalone structure and stop for supervisor ratification; on partial mutation,
   stop without repair.
3. Obtain Claude literal zero-write `PLAN PASS` for exact contract hashes, then force-add and commit
   only the five reviewed contract files.
4. At the resulting implementation base, preserve the inherited read-only Biome red against
   pristine generated `loop-fork/runs/**` evidence.
5. Add exact single-line `files.includes: ["!runs"]` with no local positive glob or copied preset
   entries; add source/test fail-open fixtures, bidirectional zero-addition checked-file accounting,
   config self-lint, local-array equality, representative inherited-exclusion-class, and
   override-byte-identity controls. Record installed parent entries without pinning them. Remove
   both invalid fixtures and the preset-scope matrix entirely within focused controls and prove no
   cache/status residue before gates.
6. Edit only `loop-fork/biome.jsonc`, run every focused and mandatory gate, commit that one file,
   and obtain Claude literal exact-SHA `PASS`.
7. Never run formatter `harness done`. Prove D6 sole active/eval-pending, record one standalone
   terminal `PASS` in task-log/meta/evals, re-prove D6 state, bookkeep separately, then return to
   unchanged D6.
