# Regression Harvest Prompt

Task `utility-workspace-root-binding` appears to record a bug fix.

Review `evals/regression/utility-workspace-root-binding.md` and convert it into a runnable regression
check when the project has a concrete eval harness for this bug class.
