# Regression Harvest Prompt

Task `atomic-global-install` appears to record a bug fix.

Review `evals/regression/mutable-global-install-symlink.md` and convert it into a runnable regression
check when the project has a concrete eval harness for this bug class.
