# Run Plan — harvto-d6-readonly-attach

Base: `ee1e7736d876d4b387f13580ec25ddf1c606e873`

1. Obtain Claude zero-write `PLAN PASS` for the canonical D6 contract and exact hashes.
2. Preserve the named exact-base red for a read-only client viewing the exact target window.
3. Bind pane-listed client identities to session-listed `client_readonly` modes, with count only as
   a cross-check; preserve fixed-arity parsing and explicit safe empty-set shim semantics.
4. Apply one classifier to the initial gate and post-send changed-suggestion branch without
   weakening pane, pipe, cursor, activity, or composer safety.
5. Prove positive, changed-suggestion, negative, replay, fail-closed, race, parser, shim, and
   exact-target isolation controls.
6. Verify, commit only tmux source/test scope, obtain Claude exact-SHA `PASS`, close once, and commit
   bookkeeping separately.

D11 durable Governess recovery transport is explicitly out of D6 scope.
