# Utility command recovery task log

- Run-98 source evidence is recorded in
  `/Users/amgad/.loop/runs/harvto-b1e274e66299/98/utility/`.
- Au Pair is receiving and claiming jobs; the issue is execution contract and
  tool adherence, not zero routing.
- Live loop-98 remains read-only and will not be restarted for this slice.
- Route intake now rejects unprofiled and non-deterministic command packets
  before creating `jobs.jsonl`, with an exact focused-check resubmission
  contract.
- Registered linked-worktree scopes, cwd, and focused-check argv now resolve
  through one verified Git worktree root. A direct-worker regression executes
  the normalized `node --check src/check.js` in that adopted checkout with
  zero model calls.
- A model-driven 600-line read returns lines 1-500 plus
  `truncated=true`, `requestedEndLine=600`, and `nextStartLine=501`; persisted
  exact-read boundaries remain fail closed.
- Focused verification passed 275/275. The first full verifier pass cleared
  lint, typecheck, build, and every sequential test file; it stopped only at
  the intentionally pending eval verdict before this evidence update.
- The final full verifier rerun passed lint, typecheck, compiled build, every
  sequential test file, and the empty baseline allowlist.
